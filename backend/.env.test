# Test Environment Variables
NODE_ENV=test

# JWT Secrets (test values)
JWT_SECRET=test-jwt-secret-for-integration-testing-only-not-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test-refresh-token-secret-for-testing-only
REFRESH_TOKEN_EXPIRES_IN=7d

# AWS Configuration (mocked for tests)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_KMS_KEY_ID=arn:aws:kms:us-east-1:123456789012:key/test-key-id

# Database (not needed for unit tests)
DATABASE_URL=postgresql://test:test@localhost:5432/test_db
