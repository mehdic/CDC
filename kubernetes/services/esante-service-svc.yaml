apiVersion: v1
kind: Service
metadata:
  name: esante-service-svc
  namespace: metapharm
  labels:
    app: esante-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: esante-service
  ports:
  - name: http
    protocol: TCP
    port: 3020
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
