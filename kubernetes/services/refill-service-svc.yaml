apiVersion: v1
kind: Service
metadata:
  name: refill-service-svc
  namespace: metapharm
  labels:
    app: refill-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: refill-service
  ports:
  - name: http
    protocol: TCP
    port: 3026
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
