apiVersion: v1
kind: Service
metadata:
  name: digital-twin-service-svc
  namespace: metapharm
  labels:
    app: digital-twin-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: digital-twin-service
  ports:
  - name: http
    protocol: TCP
    port: 3016
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
