apiVersion: v1
kind: Service
metadata:
  name: marketing-service-svc
  namespace: metapharm
  labels:
    app: marketing-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: marketing-service
  ports:
  - name: http
    protocol: TCP
    port: 3022
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
