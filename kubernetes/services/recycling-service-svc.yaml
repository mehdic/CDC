apiVersion: v1
kind: Service
metadata:
  name: recycling-service-svc
  namespace: metapharm
  labels:
    app: recycling-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: recycling-service
  ports:
  - name: http
    protocol: TCP
    port: 3025
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
