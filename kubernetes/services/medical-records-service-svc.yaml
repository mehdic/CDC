apiVersion: v1
kind: Service
metadata:
  name: medical-records-service-svc
  namespace: metapharm
  labels:
    app: medical-records-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: medical-records-service
  ports:
  - name: http
    protocol: TCP
    port: 3004
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
