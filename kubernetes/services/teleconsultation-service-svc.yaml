apiVersion: v1
kind: Service
metadata:
  name: teleconsultation-service-svc
  namespace: metapharm
  labels:
    app: teleconsultation-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: teleconsultation-service
  ports:
  - name: http
    protocol: TCP
    port: 3010
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
