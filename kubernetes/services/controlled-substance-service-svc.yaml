apiVersion: v1
kind: Service
metadata:
  name: controlled-substance-service-svc
  namespace: metapharm
  labels:
    app: controlled-substance-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: controlled-substance-service
  ports:
  - name: http
    protocol: TCP
    port: 3015
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
