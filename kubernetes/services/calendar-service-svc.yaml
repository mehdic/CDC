apiVersion: v1
kind: Service
metadata:
  name: calendar-service-svc
  namespace: metapharm
  labels:
    app: calendar-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: calendar-service
  ports:
  - name: http
    protocol: TCP
    port: 3014
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
