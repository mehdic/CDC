apiVersion: v1
kind: Service
metadata:
  name: drug-interaction-service-svc
  namespace: metapharm
  labels:
    app: drug-interaction-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: drug-interaction-service
  ports:
  - name: http
    protocol: TCP
    port: 3018
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
