apiVersion: v1
kind: Service
metadata:
  name: nurse-service-svc
  namespace: metapharm
  labels:
    app: nurse-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: nurse-service
  ports:
  - name: http
    protocol: TCP
    port: 3023
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
