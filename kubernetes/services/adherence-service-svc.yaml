apiVersion: v1
kind: Service
metadata:
  name: adherence-service-svc
  namespace: metapharm
  labels:
    app: adherence-service
    service: 'true'
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  selector:
    app: adherence-service
  ports:
  - name: http
    protocol: TCP
    port: 3011
    targetPort: http
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: ClusterIP
